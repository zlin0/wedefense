# Export model with torch.jit.script()

In this section, we describe how to export a model trained on wedefense via torch.jit.script().

If you want to use your trained model with TorchScript, you can use `torch.jit.script()` to convert your Python model into a TorchScript module.

## Example


## Notes


